info:
  name: Delete Graph Snapshot
  type: http
  seq: 38
  tags:
    - rag
    - graph
    - snapshots

http:
  method: DELETE
  url: "{{baseUrl}}/api/rag/neo4j/graph/snapshots/:id"
  params:
    - name: id
      value: "{{snapshotId}}"
      type: path
  headers:
    - name: Accept
      value: application/json
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Delete one stored snapshot by id.

runtime:
  scripts:
    - type: tests
      code: |-
          test("Delete Graph Snapshot" + " should return a successful status", function () {
            const status = res.getStatus();
            expect(status).to.be.within(200, 299);
          });

          test("Delete Graph Snapshot" + " should return JSON body", function () {
            const body = res.getBody();
            expect(body).to.be.an("object");
          });

          test("Delete Graph Snapshot" + " should have auth configured", function () {
            const authHeader = req.getHeader("Authorization") || "";
            const token = bru.getVar("token") || bru.getEnvVar("token") || bru.getSecretVar("token") || "";
            expect(Boolean(authHeader) || Boolean(token)).to.equal(true);
          });

          test("Delete Graph Snapshot" + " should include response time", function () {
            const responseTime = res.getResponseTime();
            expect(responseTime).to.be.a("number");
            expect(responseTime).to.be.at.least(0);
          });

