info:
  name: Graph Semantic Search
  type: http
  seq: 31
  tags:
    - rag
    - graph
    - search

http:
  method: GET
  url: "{{baseUrl}}/api/rag/neo4j/graph/semantic-search"
  params:
    - name: q
      value: "{{searchQuery}}"
      type: query
    - name: limit
      value: "{{graphSemanticSearchLimit}}"
      type: query
  headers:
    - name: Accept
      value: application/json
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Search nodes/entities via semantic vector search.

runtime:
  scripts:
    - type: tests
      code: |-
          test("Graph Semantic Search" + " should return a successful status", function () {
            const status = res.getStatus();
            expect(status).to.be.within(200, 299);
          });

          test("Graph Semantic Search" + " should return JSON body", function () {
            const body = res.getBody();
            expect(body).to.be.an("object");
          });

          test("Graph Semantic Search" + " should not return an error payload type", function () {
            const body = res.getBody();
            if (body && typeof body.success === "boolean") {
              expect(body.success).to.be.oneOf([true, false]);
            }
          });

          test("Graph Semantic Search" + " should have auth configured", function () {
            const authHeader = req.getHeader("Authorization") || "";
            const token = bru.getVar("token") || bru.getEnvVar("token") || bru.getSecretVar("token") || "";
            expect(Boolean(authHeader) || Boolean(token)).to.equal(true);
          });

          test("Graph Semantic Search" + " should include response time", function () {
            const responseTime = res.getResponseTime();
            expect(responseTime).to.be.a("number");
            expect(responseTime).to.be.at.least(0);
          });
