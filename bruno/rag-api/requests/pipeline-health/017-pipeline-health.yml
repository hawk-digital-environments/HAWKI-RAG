info:
  name: Pipeline Health
  type: http
  seq: 17
  tags:
    - pipeline
    - health
    - smoke

http:
  method: GET
  url: "{{baseUrl}}/api/pipeline/health"
  params:
    - name: timeout
      value: "{{pipelineHealthTimeout}}"
      type: query
  headers:
    - name: Accept
      value: application/json

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Checks runtime health of pipeline services.

runtime:
  scripts:
    - type: tests
      code: |-
          test("Pipeline Health" + " should return a successful status", function () {
            const status = res.getStatus();
            expect(status).to.be.within(200, 299);
          });

          test("Pipeline Health" + " should return JSON body", function () {
            const body = res.getBody();
            expect(body).to.be.an("object");
          });

          test("Pipeline Health" + " should not return an error payload type", function () {
            const body = res.getBody();
            if (body && typeof body.success === "boolean") {
              expect(body.success).to.be.oneOf([true, false]);
            }
          });

          test("Pipeline Health" + " should include response time", function () {
            const responseTime = res.getResponseTime();
            expect(responseTime).to.be.a("number");
            expect(responseTime).to.be.at.least(0);
          });
