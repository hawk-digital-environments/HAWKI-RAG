info:
  name: Retry Pipeline Task
  type: http
  seq: 14
  tags:
    - pipeline
    - retry

http:
  method: POST
  url: "{{baseUrl}}/api/pipeline/tasks/:taskId/retry"
  params:
    - name: taskId
      value: "{{taskId}}"
      type: path
  headers:
    - name: Accept
      value: application/json

  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Trigger retry of all failed jobs on a task.

runtime:
  scripts:
    - type: tests
      code: |-
          test("Retry Pipeline Task" + " should return a successful status", function () {
            const status = res.getStatus();
            expect(status).to.be.within(200, 299);
          });

          test("Retry Pipeline Task" + " should return JSON body", function () {
            const body = res.getBody();
            expect(body).to.be.an("object");
          });

          test("Retry Pipeline Task" + " should include response time", function () {
            const responseTime = res.getResponseTime();
            expect(responseTime).to.be.a("number");
            expect(responseTime).to.be.at.least(0);
          });

