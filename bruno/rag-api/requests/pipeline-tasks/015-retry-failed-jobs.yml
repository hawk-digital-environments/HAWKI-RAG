info:
  name: Retry Failed Jobs
  type: http
  seq: 15
  tags:
    - pipeline
    - retry

http:
  method: POST
  url: "{{baseUrl}}/api/pipeline/tasks/:taskId/retry-failed-jobs"
  params:
    - name: taskId
      value: "{{taskId}}"
      type: path
  headers:
    - name: Accept
      value: application/json

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Retry only failed jobs on a task.

runtime:
  scripts:
    - type: tests
      code: |-
          test("Retry Failed Jobs" + " should return a successful status", function () {
            const status = res.getStatus();
            expect(status).to.be.within(200, 299);
          });

          test("Retry Failed Jobs" + " should return JSON body", function () {
            const body = res.getBody();
            expect(body).to.be.an("object");
          });

          test("Retry Failed Jobs" + " should include response time", function () {
            const responseTime = res.getResponseTime();
            expect(responseTime).to.be.a("number");
            expect(responseTime).to.be.at.least(0);
          });

