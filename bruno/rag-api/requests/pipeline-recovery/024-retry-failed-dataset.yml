info:
  name: Retry Failed Dataset
  type: http
  seq: 24
  tags:
    - pipeline
    - recovery
    - retry

http:
  method: POST
  url: "{{baseUrl}}/api/pipeline/recovery/datasets/:datasetId/retry-failed"
  params:
    - name: datasetId
      value: "{{datasetId}}"
      type: path
  headers:
    - name: Accept
      value: application/json

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Retry failed jobs for one dataset.

runtime:
  scripts:
    - type: tests
      code: |-
          test("Retry Failed Dataset" + " should return a successful status", function () {
            const status = res.getStatus();
            expect(status).to.be.within(200, 299);
          });

          test("Retry Failed Dataset" + " should return JSON body", function () {
            const body = res.getBody();
            expect(body).to.be.an("object");
          });

          test("Retry Failed Dataset" + " should include response time", function () {
            const responseTime = res.getResponseTime();
            expect(responseTime).to.be.a("number");
            expect(responseTime).to.be.at.least(0);
          });

