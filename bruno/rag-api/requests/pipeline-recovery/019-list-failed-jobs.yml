info:
  name: List Failed Jobs
  type: http
  seq: 19
  tags:
    - pipeline
    - recovery

http:
  method: GET
  url: "{{baseUrl}}/api/pipeline/recovery/failed-jobs"
  params:
    - name: limit
      value: "{{failedJobLimit}}"
      type: query
    - name: taskId
      value: "{{taskId}}"
      type: query
    - name: datasetId
      value: "{{datasetId}}"
      type: query
  headers:
    - name: Accept
      value: application/json

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  List failed jobs across tasks/datasets.

runtime:
  scripts:
    - type: tests
      code: |-
          test("List Failed Jobs" + " should return a successful status", function () {
            const status = res.getStatus();
            expect(status).to.be.within(200, 299);
          });

          test("List Failed Jobs" + " should return JSON body", function () {
            const body = res.getBody();
            expect(body).to.be.an("object");
          });

          test("List Failed Jobs" + " should not return an error payload type", function () {
            const body = res.getBody();
            if (body && typeof body.success === "boolean") {
              expect(body.success).to.be.oneOf([true, false]);
            }
          });

          test("List Failed Jobs" + " should include response time", function () {
            const responseTime = res.getResponseTime();
            expect(responseTime).to.be.a("number");
            expect(responseTime).to.be.at.least(0);
          });
