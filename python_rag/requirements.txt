fastapi==0.139.2
pydantic>=2.8,<3
uvicorn[standard]==0.30.6
requests>=2.32,<3
httpx==0.27.2
temporalio>=1.10,<2
psycopg[binary]>=3.2,<4
numpy>=1.26,<2.1
neo4j==5.23.0
python-dotenv==1.2.2
raganything[all]==1.3.1
# RAG-Anything initializes the MinerU parser even for converted markdown input.
# RAWKI runs the upstream pipeline backend, not MinerU's Gradio UI. The uv
# override file also replaces RAG-Anything's broad transitive `core` request.
mineru[pipeline]==3.4.4
aiofiles>=24.1,<25
# Keep the bridge and local reranker on the same validated PyTorch release.
# The Makefile resolves separate +cpu and +cu130 deployment locks.
torch==2.13.0
# python-multipart>=0.0.18,<1.0
# Avoid PyAV source builds that require ffmpeg7 in slim/arm Docker environments.
av==12.3.0
# Keep the Git-only dependency reproducible at the revision exercised by the image tests.
git+https://github.com/HKUDS/LightRAG.git@c5bf73dbf6139f1b03f738a2fec4e47d5e66f3ab
