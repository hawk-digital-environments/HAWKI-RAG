[project]
name = "hawki-artifact-store"
version = "0.1.0"
description = "Root-confined local artifact storage for RAWKI RAG workers"
readme = "README.md"
authors = [
    { name = "Yazdan Asadi", email = "wentlerhiphop@gmail.com" }
]
requires-python = "==3.13.14"
dependencies = []

[dependency-groups]
test = [
    "pytest==8.4.2",
]

[build-system]
requires = ["uv_build==0.11.26"]
build-backend = "uv_build"
