[project]
name = "hawki-worker-runtime"
version = "0.1.0"
description = "Temporal execution, heartbeat, and logging primitives for RAWKI RAG workers"
readme = "README.md"
authors = [
    { name = "Yazdan Asadi", email = "wentlerhiphop@gmail.com" }
]
requires-python = "==3.13.14"
dependencies = []

[build-system]
requires = ["uv_build==0.11.26"]
build-backend = "uv_build"
