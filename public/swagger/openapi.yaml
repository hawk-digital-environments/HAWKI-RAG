openapi: 3.0.3
info:
  title: HAWKI RAG API
  description: Local API reference generated from currently registered routes.
  version: "1.0.0"
servers:
  - url: /api
    description: Laravel API base URL
paths:
  /ping:
    get:
      tags:
        - Health
      summary: Health ping
      responses:
        "200":
          description: Service is alive
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PingResponse"
              example:
                pong: true
  /datasets:
    get:
      tags:
        - Datasets
      summary: List datasets
      security:
        - {}
      responses:
        "200":
          description: Datasets list
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
    post:
      tags:
        - Datasets
      summary: Create dataset
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "201":
          description: Dataset created
        "401":
          $ref: "#/components/responses/Unauthorized"
  /datasets/{datasetId}:
    parameters:
      - in: path
        name: datasetId
        required: true
        schema:
          type: string
    get:
      tags:
        - Datasets
      summary: Get dataset
      responses:
        "200":
          description: Dataset details
          content:
            application/json:
              schema:
                type: object
        "404":
          $ref: "#/components/responses/NotFound"
  /documents:
    get:
      tags:
        - Documents
      summary: List documents
      responses:
        "200":
          description: Document list
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
  /documents/{documentId}:
    parameters:
      - in: path
        name: documentId
        required: true
        schema:
          type: string
    get:
      tags:
        - Documents
      summary: Get document
      responses:
        "200":
          description: Document
          content:
            application/json:
              schema:
                type: object
  /assistant/documents:
    post:
      tags:
        - Assistant Documents
      summary: Upload one PDF into a student dataset and start ingest
      description: |
        Creates one assistant document from one uploaded file.

        Mental model:
        - One `dataset_id` represents one student space.
        - That student space maps to one Qdrant collection.
        - Each uploaded PDF becomes one assistant document inside that dataset.
        - Each assistant document is chunked into many vector points during ingest.

        Use this endpoint when the assistant system is uploading a single file.
      security:
        - BearerAuth: []
      parameters:
        - in: header
          name: Idempotency-Key
          required: false
          schema:
            type: string
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
                - dataset_id
              properties:
                file:
                  type: string
                  format: binary
                dataset_id:
                  type: string
                  description: Student dataset identifier. All PDFs uploaded with the same dataset id are indexed into the same student collection.
                source_updated_at:
                  type: string
                  format: date-time
                source_checksum_sha256:
                  type: string
                graph:
                  type: boolean
                  default: false
                  description: Optional. If omitted, assistant ingest stays vector-only and does not write Neo4j graph data.
                display_name:
                  type: string
                source_url:
                  type: string
                  format: uri
                metadata_json:
                  oneOf:
                    - type: object
                      additionalProperties: true
                    - type: string
      responses:
        "202":
          description: Assistant document accepted for ingest in the target student dataset
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssistantDocumentMutationResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "422":
          description: Validation failed
  /assistant/documents/batch:
    post:
      tags:
        - Assistant Documents
      summary: Upload many PDFs into one student dataset and start one ingest workflow per file
      description: |
        Creates one assistant document per uploaded file.

        Batch behavior:
        - All files in the same request share the same `dataset_id`.
        - That means all files are indexed into the same student Qdrant collection.
        - Each PDF still becomes its own assistant document with its own task, job, and bridge document id.
        - Deleting one assistant document is intended to remove only that file's indexed outputs while keeping the other files in the dataset intact.
      security:
        - BearerAuth: []
      parameters:
        - in: header
          name: Idempotency-Key
          required: false
          schema:
            type: string
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - files
                - dataset_id
              properties:
                files:
                  type: array
                  items:
                    type: string
                    format: binary
                dataset_id:
                  type: string
                  description: Student dataset identifier shared by every file in this batch.
                graph:
                  type: boolean
                  default: false
                  description: Optional. If omitted, all assistant ingests in the batch stay vector-only and do not write Neo4j graph data.
                metadata_json:
                  oneOf:
                    - type: object
                      additionalProperties: true
                    - type: string
      responses:
        "202":
          description: All assistant documents were accepted for ingest in the same student dataset
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssistantDocumentBatchResponse"
        "207":
          description: Some assistant documents were accepted and some failed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssistantDocumentBatchResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "422":
          description: Validation failed
  /assistant/documents/{assistantDocumentId}:
    parameters:
      - in: path
        name: assistantDocumentId
        required: true
        schema:
          type: string
    get:
      tags:
        - Assistant Documents
      summary: Get assistant document status and active outputs
      description: |
        Returns the assistant document plus the currently active indexed outputs.

        The `outputs` array shows where this PDF is indexed now, including:
        - the Qdrant collection used for the dataset
        - the optional Neo4j namespace
        - the active chunk count for this document version
      security:
        - BearerAuth: []
      responses:
        "200":
          description: Assistant document
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssistantDocumentResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    put:
      tags:
        - Assistant Documents
      summary: Replace assistant document if the incoming file is newer
      description: |
        Replaces one assistant document while keeping it in the same dataset.

        Typical use case:
        - the assistant system has a newer copy of the same PDF
        - old indexed outputs for that assistant document are replaced
        - the rest of the student's dataset stays untouched
      security:
        - BearerAuth: []
      parameters:
        - in: header
          name: Idempotency-Key
          required: false
          schema:
            type: string
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                source_updated_at:
                  type: string
                  format: date-time
                source_checksum_sha256:
                  type: string
                graph:
                  type: boolean
                  description: Optional override. If omitted, the current assistant document graph setting is preserved.
                display_name:
                  type: string
                source_url:
                  type: string
                  format: uri
                metadata_json:
                  oneOf:
                    - type: object
                      additionalProperties: true
                    - type: string
                force:
                  type: boolean
      responses:
        "200":
          description: Replacement skipped because the document is unchanged
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssistantDocumentMutationResponse"
        "202":
          description: Replacement accepted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssistantDocumentMutationResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          description: Validation failed
    delete:
      tags:
        - Assistant Documents
      summary: Delete one assistant document's active indexed outputs
      description: |
        Deletes the active indexed outputs for one assistant document.

        API contract:
        - remove only this PDF's chunk points from the student dataset
        - optionally remove this PDF's Neo4j graph data
        - keep the Qdrant collection itself
        - keep every other PDF in the same dataset intact
      security:
        - BearerAuth: []
      parameters:
        - in: header
          name: Idempotency-Key
          required: false
          schema:
            type: string
      responses:
        "200":
          description: Assistant document deleted from derived indexes
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssistantDocumentDeleteResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /pipeline/tasks:
    get:
      tags:
        - Pipeline
      summary: List pipeline tasks
      responses:
        "200":
          description: Task list
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
        "401":
          $ref: "#/components/responses/Unauthorized"
    post:
      tags:
        - Pipeline
      summary: Start legacy pipeline task
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
      responses:
        "201":
          description: Task started
        "401":
          $ref: "#/components/responses/Unauthorized"
  /pipeline/tasks/start:
    post:
      tags:
        - Pipeline
      summary: Start pipeline task
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
      responses:
        "201":
          description: Task started
        "401":
          $ref: "#/components/responses/Unauthorized"
  /pipeline/tasks/{taskId}:
    parameters:
      - in: path
        name: taskId
        required: true
        schema:
          type: string
    get:
      tags:
        - Pipeline
      summary: Get task
      responses:
        "200":
          description: Task details
          content:
            application/json:
              schema:
                type: object
        "401":
          $ref: "#/components/responses/Unauthorized"
  /pipeline/tasks/{taskId}/jobs:
    parameters:
      - in: path
        name: taskId
        required: true
        schema:
          type: string
    get:
      tags:
        - Pipeline
      summary: List task jobs
      responses:
        "200":
          description: Jobs list
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
    post:
      tags:
        - Pipeline
      summary: Upsert a job
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
      responses:
        "200":
          description: Job upserted
  /pipeline/tasks/{taskId}/failed-jobs:
    parameters:
      - in: path
        name: taskId
        required: true
        schema:
          type: string
    get:
      tags:
        - Pipeline
      summary: List failed jobs for task
      responses:
        "200":
          description: Failed jobs
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
  /pipeline/tasks/{taskId}/events:
    parameters:
      - in: path
        name: taskId
        required: true
        schema:
          type: string
    get:
      tags:
        - Pipeline
      summary: Stream/events for task
      responses:
        "200":
          description: Event list
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
  /pipeline/tasks/{taskId}/retry:
    parameters:
      - in: path
        name: taskId
        required: true
        schema:
          type: string
    post:
      tags:
        - Pipeline
      summary: Retry task
      responses:
        "200":
          description: Retry initiated
  /pipeline/tasks/{taskId}/retry-failed-jobs:
    parameters:
      - in: path
        name: taskId
        required: true
        schema:
          type: string
    post:
      tags:
        - Pipeline
      summary: Retry failed jobs of task
      responses:
        "200":
          description: Retry initiated
  /pipeline/tasks/{taskId}/cancel:
    parameters:
      - in: path
        name: taskId
        required: true
        schema:
          type: string
    post:
      tags:
        - Pipeline
      summary: Cancel task
      responses:
        "200":
          description: Cancellation accepted
  /pipeline/health:
    get:
      tags:
        - Pipeline
      summary: Pipeline health status
      responses:
        "200":
          description: Pipeline health snapshot
          content:
            application/json:
              schema:
                type: object
  /pipeline/controller/files:
    post:
      tags:
        - Pipeline
      summary: Upload pipeline control files
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                file:
                  type: string
                  format: binary
      responses:
        "201":
          description: File received
  /pipeline/recovery/failed-jobs:
    get:
      tags:
        - Pipeline Recovery
      summary: List failed jobs
      responses:
        "200":
          description: Failed job list
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
  /pipeline/recovery/jobs/retry-selected:
    post:
      tags:
        - Pipeline Recovery
      summary: Retry selected failed jobs
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
      responses:
        "200":
          description: Retry queued
  /pipeline/recovery/jobs/{jobId}/retry:
    parameters:
      - in: path
        name: jobId
        required: true
        schema:
          type: string
    post:
      tags:
        - Pipeline Recovery
      summary: Retry single failed job
      responses:
        "200":
          description: Retry queued
  /pipeline/recovery/retry-all:
    post:
      tags:
        - Pipeline Recovery
      summary: Retry all failed jobs
      responses:
        "200":
          description: Retry queued
  /pipeline/recovery/tasks/{taskId}/retry-failed:
    parameters:
      - in: path
        name: taskId
        required: true
        schema:
          type: string
    post:
      tags:
        - Pipeline Recovery
      summary: Retry failed jobs for a task
      responses:
        "200":
          description: Retry queued
  /pipeline/recovery/datasets/{datasetId}/retry-failed:
    parameters:
      - in: path
        name: datasetId
        required: true
        schema:
          type: string
    post:
      tags:
        - Pipeline Recovery
      summary: Retry failed jobs for a dataset
      responses:
        "200":
          description: Retry queued
  /query:
    post:
      tags:
        - Query
      summary: Query RAG backend
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QueryRequest"
      responses:
        "200":
          description: Query response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueryResponse"
  /rag/health:
    get:
      tags:
        - RAG
      summary: Show RAG health
      responses:
        "200":
          description: Health details
          content:
            application/json:
              schema:
                type: object
  /rag/monitor:
    get:
      tags:
        - RAG
      summary: RAG monitor
      responses:
        "200":
          description: Monitor payload
          content:
            application/json:
              schema:
                type: object
  /rag/stats:
    get:
      tags:
        - RAG
      summary: RAG statistics
      responses:
        "200":
          description: Stats payload
          content:
            application/json:
              schema:
                type: object
  /rag/neo4j/graph/overview:
    get:
      tags:
        - Graph
      summary: Get graph overview
      responses:
        "200":
          description: Overview payload
          content:
            application/json:
              schema:
                type: object
  /rag/neo4j/graph/search:
    get:
      tags:
        - Graph
      summary: Search graph
      parameters:
        - in: query
          name: q
          required: false
          schema:
            type: string
      responses:
        "200":
          description: Search result
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
  /rag/neo4j/graph/semantic-search:
    get:
      tags:
        - Graph
      summary: Graph semantic search
      parameters:
        - in: query
          name: q
          required: false
          schema:
            type: string
      responses:
        "200":
          description: Search result
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
  /rag/neo4j/graph/node:
    get:
      tags:
        - Graph
      summary: Get graph node
      parameters:
        - in: query
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Node payload
          content:
            application/json:
              schema:
                type: object
  /rag/neo4j/graph/expand:
    post:
      tags:
        - Graph
      summary: Expand graph node
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
      responses:
        "200":
          description: Expanded graph data
  /rag/neo4j/graph/clear-view:
    post:
      tags:
        - Graph
      summary: Clear graph view
      responses:
        "200":
          description: View cleared
  /rag/neo4j/graph/snapshots:
    get:
      tags:
        - Graph
      summary: List graph snapshots
      responses:
        "200":
          description: Snapshot list
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
    post:
      tags:
        - Graph
      summary: Save graph snapshot
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
      responses:
        "201":
          description: Snapshot saved
  /rag/neo4j/graph/snapshots/{id}:
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
    get:
      tags:
        - Graph
      summary: Load graph snapshot
      responses:
        "200":
          description: Snapshot payload
          content:
            application/json:
              schema:
                type: object
    delete:
      tags:
        - Graph
      summary: Delete snapshot
      responses:
        "204":
          description: Deleted
  /rag/neo4j/clear:
    post:
      tags:
        - Graph
      summary: Clear Neo4j graph content
      responses:
        "200":
          description: Graph cleared
components:
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
  responses:
    Unauthorized:
      description: Unauthorized
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    NotFound:
      description: Resource not found
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
  schemas:
    Error:
      type: object
      properties:
        message:
          type: string
        status:
          type: integer
    PingResponse:
      type: object
      properties:
        pong:
          type: boolean
    QueryRequest:
      type: object
      required:
        - query
      properties:
        query:
          type: string
    QueryResponse:
      type: object
      properties:
        answer:
          type: string
        sources:
          type: array
          items:
            type: object
    AssistantDocumentOperation:
      type: object
      properties:
        type:
          type: string
        status:
          type: string
        reason:
          type: string
          nullable: true
    AssistantPipelineReference:
      type: object
      properties:
        task_id:
          type: string
          nullable: true
        job_id:
          type: string
          nullable: true
        source_id:
          type: string
          nullable: true
    AssistantDocumentOutput:
      type: object
      description: One active indexed output for one assistant document version.
      properties:
        bridge_document_id:
          type: string
          description: Python bridge document id used for downstream delete and replacement operations.
        qdrant_collection:
          type: string
          description: Qdrant collection that holds this document's chunk points. In the assistant model, this is the student dataset collection.
        neo4j_namespace:
          type: string
          nullable: true
          description: Neo4j namespace used for graph writes when graph ingest is enabled.
        chunk_count:
          type: integer
          description: Number of active chunks currently indexed for this document output.
        status:
          type: string
        active:
          type: boolean
        indexed_at:
          type: string
          format: date-time
          nullable: true
    AssistantDocumentResource:
      type: object
      description: Assistant-facing view of one uploaded file and its indexing state.
      properties:
        assistant_document_id:
          type: string
        dataset_id:
          type: string
          description: Student dataset identifier. Multiple assistant documents can share this dataset and therefore share the same Qdrant collection.
        status:
          type: string
        graph_enabled:
          type: boolean
          description: Whether new ingests for this assistant document should also write graph data to Neo4j.
        display_name:
          type: string
          nullable: true
        source_type:
          type: string
        source_url:
          type: string
          nullable: true
        source_updated_at:
          type: string
          format: date-time
          nullable: true
        source_checksum_sha256:
          type: string
          nullable: true
        latest_document_version:
          type: string
          nullable: true
        indexed_at:
          type: string
          format: date-time
          nullable: true
        latest_task_id:
          type: string
          nullable: true
        latest_job_id:
          type: string
          nullable: true
        latest_source_id:
          type: string
          nullable: true
        last_error:
          type: string
          nullable: true
        metadata_json:
          type: object
          nullable: true
          additionalProperties: true
        outputs:
          type: array
          items:
            $ref: "#/components/schemas/AssistantDocumentOutput"
          description: Active indexed outputs for this assistant document. A single uploaded PDF can produce many chunks but still appears here as one document output record.
        active_output_count:
          type: integer
        active_chunk_count:
          type: integer
          description: Total active chunks currently indexed for this assistant document across its active outputs.
    AssistantDocumentResponse:
      type: object
      properties:
        success:
          type: boolean
        document:
          $ref: "#/components/schemas/AssistantDocumentResource"
    AssistantDocumentMutationResponse:
      type: object
      properties:
        success:
          type: boolean
        operation:
          $ref: "#/components/schemas/AssistantDocumentOperation"
        document:
          $ref: "#/components/schemas/AssistantDocumentResource"
        pipeline:
          $ref: "#/components/schemas/AssistantPipelineReference"
        message:
          type: string
        error:
          type: string
          nullable: true
    AssistantDocumentBatchItem:
      type: object
      properties:
        index:
          type: integer
        file_name:
          type: string
        http_status:
          type: integer
        success:
          type: boolean
        result:
          type: object
          additionalProperties: true
    AssistantDocumentBatchResponse:
      type: object
      description: Batch upload result. Each accepted file becomes a separate assistant document in the same dataset.
      properties:
        success:
          type: boolean
        operation:
          $ref: "#/components/schemas/AssistantDocumentOperation"
        summary:
          type: object
          properties:
            total:
              type: integer
            accepted:
              type: integer
            failed:
              type: integer
        items:
          type: array
          items:
            $ref: "#/components/schemas/AssistantDocumentBatchItem"
    AssistantDocumentDeleteBreakdown:
      type: object
      description: Per-store delete results for one assistant document.
      properties:
        bridge_documents_deleted:
          type: integer
          description: Number of bridge document outputs targeted by the delete operation.
        qdrant:
          type: array
          items:
            type: object
            additionalProperties: true
          description: Qdrant delete results, typically one entry per active bridge document, including the affected collection and deleted point count.
        neo4j:
          type: array
          items:
            type: object
            additionalProperties: true
          description: Neo4j delete results, typically one entry per active bridge document when graph ingest was enabled, including the affected namespace.
    AssistantDocumentDeleteResponse:
      type: object
      description: Response returned after deleting one assistant document's active indexed outputs.
      properties:
        success:
          type: boolean
        operation:
          $ref: "#/components/schemas/AssistantDocumentOperation"
        document:
          $ref: "#/components/schemas/AssistantDocumentResource"
        deletion:
          $ref: "#/components/schemas/AssistantDocumentDeleteBreakdown"
